server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security_demo?useSSL=false
    username: root
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: secreto-super-seguro-para-jwt
  expiration: 3600000 # 1 hora

logging:
  level:
    org.springframework.security: DEBUG
